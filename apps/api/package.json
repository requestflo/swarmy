{
  "name": "@swarmy/api",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "bun --env-file=../../.env run --watch src/index.ts",
    "build": "bun build src/index.ts --outdir dist --target bun",
    "start": "bun run src/index.ts",
    "typecheck": "tsc --noEmit",
    "clean": "rm -rf dist .turbo",
    "restore": "bun run src/restore.ts",
    "reset-2fa": "bun run src/reset-2fa.ts",
    "test": "bun test"
  },
  "dependencies": {
    "@modelcontextprotocol/server": "2.1.0",
    "@swarmy/api-rest": "workspace:*",
    "@swarmy/auth": "workspace:*",
    "@swarmy/core": "workspace:*",
    "@swarmy/db": "workspace:*",
    "@swarmy/devkit": "workspace:*",
    "@swarmy/ingress": "workspace:*",
    "@swarmy/trpc": "workspace:*",
    "@trpc/server": "^11.0.0",
    "hono": "^4.6.14",
    "nodemailer": "^7.0.0"
  },
  "devDependencies": {
    "@modelcontextprotocol/client": "2.1.0",
    "@types/bun": "latest",
    "@types/nodemailer": "^7.0.0",
    "typescript": "^5.5.4"
  }
}
