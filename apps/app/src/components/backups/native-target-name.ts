/** Name of the managed destination on the in-swarm Garage store (mirrors
 *  `NATIVE_TARGET_NAME` in packages/trpc backups.service — the app never
 *  imports server code). */
export const NATIVE_TARGET_NAME = 'swarmy-object-storage';
