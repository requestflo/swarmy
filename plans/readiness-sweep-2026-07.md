# Readiness sweep: test every feature against a live (non-demo) backend

**Goal (updated 2026-07-11 per the user's `/goal` directive):** determine whether swarmy can
actually be *deployed* by a real user — through the dashboard UI, the literal one-line install
command, and the deploy/blueprint flow only. No code changes, no env-var tweaks, no SSH/manual
fixes to make a test pass: any of those is itself a FAIL to document, not a valid path to green.
Start small (ingress + a browser-reachable app locally), then scale to DigitalOcean droplets across
regions to test global DNS/geo-routing for real. Stop and write up precisely where/why whenever
genuinely blocked.

**Environment:** `swarmy-node-1` (manager) + `swarmy-node-2` (worker), both Lima VMs, mesh-joined
via NetBird, enrolled to org `Calum MacRae's team` (`orgId` starts `hRQ9kThFFZIy...`) through the
dashboard's real "Add a node" install-script flow — not `scripts/mint-token.ts` (hardcoded to the
old `swarmy-dev` org) and not seeded/demo data.

## Methodology correction (2026-07-11)

The first pass of this sweep (rows 1-2 below, done in an earlier segment) used manual SSH recovery
(`rejoin --force`, hand-editing swarm state, manual `docker swarm join`) to get nodes healthy after
hitting bugs. That is a testing-methodology mistake, not a success — see
[[manual-recovery-required-not-magical]]. **From this point on, testing goes through the product
surface only** (dashboard UI, the literal one-line install command, the deploy UI). If a step needs
SSH to fix (not just to read logs), that surface fails, gets written up immediately, and testing
does not route around it by hand.

Acting on that correction, the polluted two-node environment was torn down and a **fresh** Lima VM
was provisioned, enrolled using nothing but the exact command the dashboard's "Add a node" flow
generates (obtained by actually driving that UI, not hand-constructed). Result: **it fails at the
very first step, for every fresh node, unconditionally.** See row 1 below — this reverses the
earlier "✅ Done" status, which was only true because it was reached by hand-fixing, not because the
real flow works.

## Status

| # | Feature | Status | Notes |
|---|---|---|---|
| 1 | Node onboarding via the real dashboard one-liner | ✅ **Fixed and verified (2026-07-11)** | Was blocked 100% of the time by a GHCR `denied` pull. User explicitly authorized the one-line fix documented in [[install-defaults-to-unpullable-ghcr-image]] (repoint the dashboard one-liner from `/install.sh` to the already-live, already-tested `/install/loader.sh`). Verified on a fresh Lima VM through the real, unmodified-except-for-that-one-line dashboard flow: Docker installs, systemd backend auto-selected, agent installs/starts/registers, mesh joins. No manual SSH steps needed for this stage. |
| 2 | New nodes reach a working swarm | ✅ **Confirmed working for a clean org (2026-07-11)** | Root cause: this org's `swarm_config` DB row pointed at a dead manager address from an earlier, since-destroyed test VM — every new node silently tried to "join" it instead of falling back to `init`. Fully root-caused in [[stale-swarm-config-blocks-new-nodes-after-manager-loss]]. **Retested against a genuinely fresh org** ("Sweep Tester's team", created via the real signup flow — no in-app multi-org support exists, so a new account was the only product-surface path to an unpolluted org) on a fresh Lima VM: the real, unmodified dashboard one-liner reached a healthy single-manager swarm with zero manual intervention. Confirms the bug is stale-manager-pollution-specific, not a defect in the init path itself — **the bug itself is still open and unfixed** (a real production risk for any org that permanently loses its manager), but testing continues under this fresh org since it's clean. |
| 3 | Static-site blueprint deploy (stacks/deploy pipeline) | ✅ **Works** | Deployed via the real Deploy → Blueprints flow to the fresh org's healthy 1-node swarm: converged cleanly, 1/1 replicas, zero manual intervention. The deploy pipeline itself is solid — see row 8 for what happened next (ingress). |
| 4 | Issue documentation | ✅ Done (ongoing) | 9 issues written so far in `issues/`. |
| 5 | This plan | ✅ Done (ongoing) | |
| 6 | Remaining feature sweep (backups-dr, CI/CD+registry, managed-data-services, observability/OTEL, geo-edge-routing/DNS, auth/ABAC, REST API surface, node recovery, terminal/web-SSH, licensing) | ⏳ Next up | Ingress (row 8) is done. Continuing the sweep feature-by-feature through the same fresh org/node, still via product surface only. |
| 7 | Scratch file cleanup | ⏳ Pending | `apps/api/diag-org.ts`, `apps/api/diag-org2.ts` still present, untracked. |
| 8 | Ingress: get one browser-reachable, secured route ("start small" milestone) | ❌ **FAIL — fully root-caused, documented** | Deployed the static-site blueprint, then drove the real Platform → Edge & ingress UI through every legitimate step (driver select, Enabled toggle, the separate "Deploy / converge controller" button, the separate "Target nodes" toggle). Result: **total unreachability** at every stage, confirmed three independent ways (direct node/Docker inspection, SSH-port-forwarded curl, an actual Chrome navigation), despite the dashboard confidently showing "1/1 SECURED" / "TLS auto" throughout. Four stacked root causes, one of them a deterministic, always-reproducible Docker Swarm placement-constraint bug (wrong node-ID namespace) that makes the "pin ingress to one node" path permanently unschedulable for every org, always. Full writeup: [[ingress-never-actually-serves-traffic]]. This is the literal milestone the governing directive named as the first thing to prove out, and it is not achievable today through the product surface alone. |
| 9 | Managed databases (Postgres primary/replica, via the "Node API" blueprint and the Data tab) | ❌ **FAIL — hard blocker, fully root-caused** | Blueprint deploy converges the stack shell but the Postgres primary/replica crash-loop forever: `bitnami/postgresql:16` no longer exists on Docker Hub (Bitnami deprecated versioned free-tier tags). Confirmed via direct `docker pull`, Docker Hub's tag API, and the exact source line (`manageddb.service.ts:312`). Checked every reachable product surface (existing DB's "Topology & backups" panel, "Add another database" provisioning form) for an image-tag override — **none exists**. Hard, unconditional blocker for the entire managed-database feature area, directly on point with the governing directive's explicit "the kind of database stuff" priority. Full writeup: [[managed-postgres-bitnami-image-tag-16-dead]]. |
| 10 | Dashboard session stability under normal use | ❌ **FAIL — reproduced 5x live, mechanism root-caused** | Session silently dies and force-redirects to `/login` during completely ordinary navigation (nav clicks, stack sub-tab clicks, plain URL nav) — no idle period needed, reproduced within seconds of a fresh login. Mechanism fully traced: a 60s `cookieCache` (`packages/auth/src/server.ts:154`) forces frequent live session re-checks, and every route navigation's `beforeLoad` guard (`apps/app/src/routes/_authed.tsx:7-15`) redirects instantly and unconditionally on any empty check, with no retry/grace period and no 401 interceptor anywhere in the tRPC client. The deeper "why does the live check fail so early" question is still open (needs session-table inspection). Full writeup: [[dashboard-session-dies-under-normal-use]]. |
| 11 | Native S3/object-storage backups (swarmy-managed Garage cluster) | ❌ **FAIL — hard blocker, fully root-caused** | Platform → Backup destinations → "Configure Garage" (the documented first step to enable swarmy's own S3 storage) throws an unhandled `null is not an object (evaluating 'row.memberNodeIds')` and silently fails, for every org, on the very first attempt — before a `StorageCluster` row exists. Root cause: `replicatedStore.service.ts:209` calls `members(existing as ClusterRow)` where `existing` is genuinely `null` for a first-time org, and `members()` (line 85-87) never null-checks its `row` param. Deterministic, unconditional, every time. Full writeup: [[replicated-object-store-crashes-on-first-configure]]. |
| 12 | External S3-compatible backup destination ("Add destination" form) | ✅ **Works** | Separate code path from the crashing Garage flow above. Filling all required fields (NAME, ENDPOINT, BUCKET — KIND left default "S3-compatible", ACCESS KEY ID/SECRET ACCESS KEY as dummy test values, PREFIX/REGION left blank since marked optional) and submitting produced a "Destination added" toast and a new entry in the DESTINATIONS list (kind `S3`, status `keyed`). Confirms the destination-CRUD layer itself is sound; the Garage bug is scoped specifically to swarmy's own self-hosted object-store provisioning, not backup destinations generally. Note: an earlier submit attempt with only ACCESS KEY ID/SECRET filled (NAME/ENDPOINT/BUCKET left as unfilled placeholder text) silently no-opped — no toast, no list entry, no visible validation error near the empty required fields. That's a minor UX gap (silent no-op instead of a "required field" message) but not severe enough for its own issue file. Backup-execution path against this destination confirmed working — see row 14. |
| 13 | Cache provisioning (Valkey/Redis, stack Data tab) | ✅ **Works — good UX** | One-click "Add cache" on the `littleworld` stack's Data tab provisioned a real Valkey service (confirmed via node inspection: `littleworld_main-cache` Docker service, `1/1` replicas, plus its backing named volume `littleworld_main-cache-data`). One-time-reveal password stored in a Docker secret (`swarmy-cache-littleworld-main-password`), with clear "can't be shown again, copy it now" UI messaging — a strong positive example of the "magical" UX bar the governing directive asks for. |
| 14 | Ad-hoc volume backup execution ("Back up now" on a stack's Backups tab) | ✅ **Works** | Targeted the real `littleworld_main-cache-data` volume (row 13) at the "Test S3 destination" from row 12. Submission correctly drove SNAPSHOTS 0→1, status went `RUNNING` then `FAILED` a few seconds later — expected and correct, since the destination endpoint (`https://s3.example.com`) is deliberately fake with no real backing service. Confirms the backup-execution mechanism itself (not just destination CRUD) is fully wired and functional end-to-end, and correctly detects/reports real failures rather than silently succeeding or crash-looping. One UX gap found along the way: the failed row shows no error detail anywhere in the dashboard, even though the real restic error is already captured server-side and already on the wire via `listSnapshots` — a frontend-only gap, not fixed this session. Full writeup: [[backup-failure-reason-captured-but-never-shown]]. |
| 15 | Dashboard session stability — 6th live reproduction | ❌ **FAIL — same bug as row 10, new evidence** | Reproduced again while testing row 13/14, this time with **zero navigation involved** — session died silently while sitting on one already-loaded page (`/stacks/littleworld/data`), surfacing only as background tRPC calls 401ing (24+ consecutive 401s observed via network inspection) with no `beforeLoad` redirect firing since no route change occurred to trigger it. Rules out "only happens on route-change timing" as the trigger; strengthens the case that the underlying live-session-check failure is time-based, not navigation-triggered. Added to the existing writeup: [[dashboard-session-dies-under-normal-use]]. |
| 16 | Dashboard session stability — 7th live reproduction | ❌ **FAIL — same bug** | Reproduced again immediately after row 15, this time caught in the act: a "Resilience" score card showed `UNAUTHORIZED` while the rest of the page was still rendering normally (session already dead, no symptom yet), then the very next navigation (`/backups`, a plain sidebar click) bounced straight to `/login` — Chrome autofill again suggested the stale `calum+swarmytest@gomacrae.com` instead of the active `calum+swarmytest3@gomacrae.com`, exactly the compounding usability problem already documented. 7th reproduction in this sweep. No new root-cause evidence beyond confirming the pattern is highly reproducible under ordinary continued use — did not re-edit the issue file for this repro since it adds no new mechanism, only frequency. |
| 17 | "Node path" backup destination (local filesystem, no cloud account) | ❌ **FAIL — critical, root-caused** | Created a "Node path" destination (Base path `/srv/backups`, Prefix `swarmy-test`) via the real "Add destination" form. First red flag: the new entry's kind badge showed **"S3"**, not "Node path". Ran an ad-hoc backup against it (same `littleworld_main-cache-data` volume as row 14) — went `RUNNING` → `FAILED` in seconds. Node-filesystem check confirmed `/srv/backups` was never created — nothing was ever written to disk. Direct DB query of the `snapshot.error` column (not visible anywhere in the UI — this is exactly the gap from row 14/[[backup-failure-reason-captured-but-never-shown]]) revealed the real restic error: it tried to resolve `srv` as a DNS hostname and open `s3://srv/backups/swarmy-test` — i.e. the node-path destination is being silently treated as an S3 destination. Root-caused to a case-mismatch bug: `repoUrl()` compares `row.kind === 'node'` (lowercase) but the DB always stores the Prisma enum uppercase (`'NODE'`/`'S3'`), so the check never matches and every node-path destination falls through to the S3 branch, every time, for every org. **This means the only backup destination kind that currently works at all in swarmy is external S3** (row 12) — swarmy's own "no cloud account, no egress" pitch cannot currently be honored for backups on a local/single-node deployment like this one. Full writeup: [[node-path-backup-destination-always-treated-as-s3]]. |
| 18 | Dashboard session stability — 8th live reproduction | ❌ **FAIL — same bug, sharper timing evidence** | Reproduced again within roughly 1-2 minutes of the fresh login that followed row 16's reproduction — a plain `navigate()` to `/stacks` bounced straight to `/login` with no interactive session in between beyond a couple of page loads. 8th reproduction in this sweep, and the fastest yet: the session is not surviving even a couple of minutes of very light use immediately after a fresh login. No new root-cause mechanism; added as a data point to [[dashboard-session-dies-under-normal-use]] (see "7th and 8th reproductions" section) since it sharpens the timing evidence (well under one `cookieCache.maxAge` cycle) without changing the diagnosis. Given 8 reproductions now fully documented with two independent pieces of mechanism evidence (redirect-guard root cause, and a live network trace showing the session dies with zero navigation), this bug is being treated as sufficiently proven — further live reproductions are not being separately logged going forward unless a genuinely new symptom appears. A 9th reproduction (near-instantaneous, zero interactive time) is logged only in the issue file per that same decision. |
| 19 | Sweep's first genuinely successful backup — local S3-compatible destination | ✅ **Works — unblocked via a real deploy, not a workaround** | Both prior backup-destination kinds were dead ends for a credential-free local test: swarmy's own Garage object store crashes on first configure (row 11), and "Node path" is unconditionally broken (row 17), leaving external S3 (row 12) as the only kind proven to work — but only with a real cloud account, contradicting the "no cloud account, no egress" pitch. Unblocked by deploying `adobe/s3mock:latest` (a lightweight local S3-compatible mock) through swarmy's own real "Ship a service" form (port 9090 published) — a legitimate use of the product surface, not a workaround around swarmy's own code. Verified genuinely running via direct node inspection (`docker ps`/`docker service ls`: 1/1, `*:9090->9090/tcp`) despite the service detail page's Containers panel simultaneously showing "0 / Nothing running yet" — a new, separate display bug, not yet its own issue file. Added it as a new "Local s3mock" destination via the real "Add destination" form (dummy `test`/`test` credentials, since the backing service has no real auth). Triggered "Back up now" against `littleworld_main-cache-data` — went `RUNNING` → **`SUCCEEDED`**, 177 B. First fully successful snapshot in this entire sweep. |
| 20 | Restore-from-snapshot | ✅ **Works** | Clicked "Restore" on row 19's succeeded snapshot, destination volume left as the pre-filled default (`littleworld_main-cache-data`, i.e. restore-in-place). Modal showed "Restoring…" then closed with no visible toast either way (a minor UX gap — no explicit success confirmation), and `POST /api/trpc/backups.restoreSnapshot` returned `200`. Verified for real via direct node filesystem inspection rather than trusting the silent UI: `stat` on the restored volume's data file showed `Modify` time unchanged from original creation (`11:18:15`, consistent with restic preserving the snapshot's recorded mtime) but **`Change` (ctime) time updated to `11:46:15`** — the exact moment of the restore click — proving the file's content was genuinely rewritten to disk, not a UI no-op. First successful restore-from-snapshot in this entire sweep. |
| 21 | Scheduled volume backups (per-stack "New schedule") | ✅ **Works** | Created a real recurring schedule via the actual UI (`littleworld_main-cache-data`, EVERY 3 MINUTES, destination "Local s3mock") — not a workaround, the real "New schedule" form. Verified genuinely firing via background DB polling (`select count(*) from snapshot;` every 15s) rather than trusting the UI countdown: snapshot count went 3→4 exactly at the expected 3-minute mark. Follow-up query confirmed the new row (`cmrg8wrl300374vsbf225uom7`) has `status=SUCCEEDED`, `targetId` = the same "Local s3mock" destination, `sizeBytes=177` — identical to row 19's manual backup, proving the scheduler executes real, correct work, not just a UI-visible countdown. First confirmation this sweep that the cron/scheduling mechanism actually does something. |
| 22 | Controller backup ("Protect the brain" — backs up swarmy's own control-plane DB, not user volumes) | ❌ **Completely broken — critical** | Generated/saved a restore passphrase via the real UI flow, enabled the schedule, clicked "Back up now". Red toast: `Executable not found in $PATH: restic`. Confirmed in `controller_snapshot` table: one `FAILED` row, `startedAt`/`finishedAt` ~186ms apart (immediate PATH-lookup failure, not a real attempt), `error=Executable not found in $PATH: "restic"`. Root-caused via source: unlike volume backups (which run restic in a Docker sidecar on the node), controller backups try to `spawn('restic', ...)` **directly on the controller process**, and the controller's own `apps/api/Dockerfile` (`oven/bun:1.3-alpine`) never installs a `restic` binary — a packaging gap present in every deployment, not a misconfiguration. See [[controller-backup-restic-not-found-in-path]] for full call-chain citation. This is swarmy's entire disaster-recovery story for its own control-plane DB, and it cannot succeed once, for any org, as shipped. |
| 23 | CI & builds — In-swarm registry toggle | ⚠️ **UI says "Live", container genuinely runs, but published port unreachable** | Toggled on via the real Deploy → CI & builds page. Confirmed via node inspection: a real `registry:2` service is running 1/1, and the registry itself works correctly when queried from inside its own container (`{}` on `/v2/`). But the published port (`*:5000`) times out from the host, and from the container's own overlay IP. Differential test (throwaway vanilla `nginx:alpine` service, diagnostic-only, not part of swarmy) proved this is **Docker Swarm's routing mesh itself failing in this Lima VM**, not a registry- or swarmy-specific bug — see [[swarm-routing-mesh-unreachable-in-lima-vm]]. Needs retesting on a real DigitalOcean droplet before drawing any conclusion about whether this affects real deployments; local push/pull to the in-swarm registry cannot be verified until then. |
| 24 | Backup destination schedule deletion (cleanup of row 21's test schedule) | ✅ **Works, good UX** | Deleted the 3-minute test schedule via the real "trash" icon on the stack Backups tab. Clean confirmation modal ("Stop backing up littleworld_main-cache-data? The schedule is deleted and no new snapshots are taken. Existing snapshots stay in the destination."), removed cleanly, no lingering cron activity observed after. |
| 25 | CI & builds — "Link a repo" form | ✅ **Works** | First attempt was masked by [[dashboard-session-dies-under-normal-use]] firing mid-submission (generic "UNAUTHORIZED" toast, not a real feature error). Retested cleanly after relogin: filled Repository URL/Access Token via `form_input` (Chrome autofill kept trying to inject a stale saved email into the URL field and the old dummy token into the token field — same compounding autofill quirk as the login flow, not a swarmy bug), submitted, got a clean `200` on `cicd.addRepo`. Repo shows correctly in the "Repositories" list ("1 WATCHED") and the page header updates to "1 repo wired." No client-side validation of the git URL/token shape is attempted — it stores whatever is given. |
| 26 | CI & builds — "Build" trigger | ❌ **Always fails — no way to enable it** | Clicked "Build" on the just-linked repo. Immediate red toast: `build disabled on this agent`. Root-caused via source: the agent gates all build commands behind `SWARMY_ALLOW_BUILD`, which defaults to `false` and is never set by the dashboard's generated install one-liner, never exposed as a node-role toggle in the UI, and undocumented outside a local-dev-only script. There is no product-surface way to turn builds on for any node. See [[ci-build-trigger-always-fails-no-enable-path]]. |
| 27 | CI & builds — Image GC "Save policy" | ✅ **Works** | Clicked "Save policy" with default Mode ("Promote on healthcheck") and "Keep prod images" on. Clean `200` on `cicd.setGcPolicy`, green "GC policy saved" toast. |
| 28 | CI & builds — Image policy toggles | ✅ **Works** | Toggled "Block critical vulnerabilities" on. Clean save, green "Image policy updated" toast, toggle state persisted visually. |
| 29 | CI & builds — Org signing key generation | ❌ **Real backend bug — cosign container can't write its key** | Clicked "Generate key" under Image policy. Button showed "Generating…" then reverted, with a red error toast: `cosign generate-key-pair exited 1: Error: open /keys/cosign.key: permission denied`. Toast wiring itself is correct (`scan-policy-card.tsx` has a proper `onError` handler; initial "silent failure" read was a screenshot-timing miss, not a real UX gap). Root-caused via source: the cosign key volume is a fresh Docker named volume, auto-created root-owned, and the official `cosign` image runs as a non-root UID with no way to override it in the `runOnce` protocol — a straight UID/ownership mismatch. See [[org-signing-key-cosign-permission-denied]]. |
| 30 | Observability — "Store" status / logs-traces-metrics reads | ❌ **Real backend bug — dashboard reads use the wrong ClickHouse hostname** | Enabled Observability on the swarmy-system stack. Collector shows "Running", Store stuck on "Pending" indefinitely (10+ min). Verified via node inspection that the collector is genuinely connected and writing to ClickHouse (`"Everything is ready. Begin running and processing data"` in its logs) — the pipeline itself works. Root-caused via source: the collector's own exporter config correctly uses the real Swarm service name `swarmy-clickhouse` (confirmed by an existing passing test), but the DSN saved to the DB and used for *every* dashboard read — the "Store" health ping and all logs/traces/metrics/service-map queries — is built by a separate function that hardcodes the literal hostname `clickhouse`, which is never a valid Swarm DNS name for this service (no alias configured) and can never resolve. Confirmed this reproduces in real deployments, not just this test VM: the production `controller` service is on the same overlay network in `deploy/swarmy.standard.stack.yml`, so the same hostname mismatch applies there too. Net effect: telemetry is silently collected but can never be read back or confirmed healthy from the dashboard. See [[observability-store-hostname-mismatch-always-unreachable]]. |
| 31 | Platform → Edge & ingress — "swarmy is your nameserver" (geo-DNS) local smoke test | ✅ **Works as designed, correctly gated on missing preconditions** | Toggled "Serve DNS" on (GeoIP: DB-IP built-in), then added a zone (`swarmytest-sweep.example`) via the real "Add zone" form — clean success toast, zone appears with derived `apex`/`www` records (both "0/0 healthy," expected with zero backing endpoints). UI correctly surfaces two clear warnings rather than silently pretending to work: "No ingress+outlet endpoints with a public IP exist yet — geo hosts will answer empty" and "No eligible nameserver nodes — a nameserver node needs the ingress and outlet roles, a region, and a public IP." Ran the built-in "Resolution preview" tool (runs the real answering engine) against the new zone — correctly returned "unsteered / No answers," matching the "no eligible endpoints" state. This single Lima VM already shows a correctly auto-detected public IP and `us-east` region under "Node regions & public IPs," but has no ingress/outlet role assigned, so this is an expected, correctly-communicated precondition gap, not a bug. Genuine multi-region geo-steering validation needs real distinct-region nodes with real public IPs — deferred to the planned DigitalOcean multi-region phase per the governing directive, not meaningfully testable further on one local VM. |
| 32 | Governance → Access & roles → Policies (ABAC engine) | ✅ **Works** | Confirmed all three legs of the feature against the live backend, not just the UI shell. (1) The five default policies (`Owners can do anything` @100, `Admins can do anything` @90, `Members can read` @50, `Resource operators can operate their resources` @45, `Members can run safe operations` @40) render correctly with effect/priority/status. (2) Simulator ("Can I do this?") ran a real live decision-path query for `service.restart` and correctly returned `permit — permitted by policy "Owners can do anything" (policy cmrga9loo004z4vsbjkmfpfc6)`, i.e. it's genuinely evaluating against the org's actual policy set for the signed-in identity, not a canned response. (3) Created a real custom policy via the builder form (name "Sweep test deny", effect `forbid`, action `service.restart`, role `member`) — "Generate JSON" correctly compiled the form into the rule JSON, "Save policy" persisted it and the form reset, and the new row appeared immediately in the Policies table with the correct effect/priority/status. Deleted it via the row's trash icon — deletion succeeded immediately, table returned to exactly the 5 defaults. No dashboard, install-script, or SSH involvement needed; this is a fully product-surface, fully functional feature. Not yet tested: whether a `forbid` policy actually blocks a non-owner member end-to-end (would require a second real user account in a non-owner role to verify against, not just the simulator) — deferred, not required to establish the CRUD+simulate path works. |
| 33 | Governance → Guardrails — enforcement on the "Ship a service" quick-deploy path | ❌ **Critical — guardrails are completely bypassed by this deploy path** | Armed "Production safety mode", left "No :latest images in production" set to Block, marked the `apitest` stack production via the real "Mark production" button (confirmed via toast), then deployed `nginx:latest` into `apitest` via the real "Ship a service" form (`/services/new`). The deploy succeeded fully and visibly — real container, real nginx boot logs — with zero block, zero warning toast, and "Recent decisions" still reading "Nothing blocked or overridden yet" afterward. Cross-checked the Audit log's "Who deployed?" filter too — no entry at all for this deploy, not even a benign create record. Root-caused via source: `createService` (`packages/trpc/src/services/service.service.ts:159-192`), the mutation backing `/services/new`, dispatches straight to `service.deploy` and never calls `evaluateAdmission` (the shared governance chokepoint) at all — no import of it or of `writeAudit` anywhere in the file. This is a missing-integration gap, not a logic bug: the guardrail engine itself, `effectiveRules()`'s prod-forcing, the `:latest` check, "Mark production"'s label persistence, and the audit-log writer are all confirmed correct in isolation and are correctly wired into the two *other* deploy paths (`deployFromCompose` in `stack.service.ts:187` and even the canary path `startCanary` in `releases.service.ts:815`, which has an explicit comment noting "a canary is a service deploy — the admission pipeline still applies"). `createService` is the one deploy-shaped mutation left out. See [[guardrails-bypassed-by-quick-deploy-service-create]]. |
| 34 | Governance → Cost & capacity | ✅ **Works** | Set a real monthly price ($40) on the sole node via the "Set cost" inline editor — persisted correctly (confirmed via full page reload: header changed to "$40/mo — $22 doing work", "1/1 nodes priced"). Per-stack estimate correctly recalculated proportional dollar costs from live CPU/memory share (swarmy-system $10.60/mo, littleworld $6.60/mo, ungrouped $4.14/mo, apitest $0.29/mo) instead of staying at the earlier flat $0/mo placeholder. "Recommendations" panel correctly converted its CPU-percentage idle-service list into real dollar savings estimates once a price existed (e.g. "swarmy-clickhouse uses 0.84% CPU — scale it down or enable scale-to-zero (frees ~$9.99/mo)"), and correctly picked up the `sweep-latest-violation` service created earlier this segment as a new idle-service recommendation, showing the feature reads live service state rather than a cached snapshot. Fully functional, product-surface only. |
| 35 | Node repair one-liner ("Repair this node" panel, degraded node in a second org) | ❌ **Critical — the dashboard's own generated command doesn't work as instructed, on any node but the controller's own host** | A degraded node's "Repair this node" panel gives a one-liner with the caption "swap localhost for the controller's reachable address" — followed that instruction exactly over SSH and it still failed, twice, with different errors (`curl: (7) ... Couldn't connect to server` against the dashboard's own port, then `failed to download installer` against the API port). Root-caused via source: the install/repair flow is two-stage (loader.sh → install.sh → agent binary), and stages 2 onward all re-derive their own target URL from a single static server-side `CONTROLLER_PUBLIC_URL` config value (default `http://localhost:3021`), never from the address actually used to reach them — swapping the *outer* curl's address (the only thing the generated command lets you override) does nothing for the other three URLs baked in behind it. Confirmed the fix works end-to-end: manually supplying three undocumented env var overrides (`SWARMY_INSTALLER_URL`, `SWARMY_CONTROLLER_URL`, `SWARMY_BINARY_BASE_URL`) got the same node all the way through binary install, systemd setup, and mesh join (`swarmy-agent doctor --repair` reporting everything healthy except an unrelated expired join token — confirmed on Settings → Join tokens as `0/1 uses, expired`, i.e. time-based TTL, not single-use consumption). Corroborated independently: `scripts/local-vms.sh`'s own `enroll_node()` has a code comment describing this exact problem and threading the same two of the three overrides as a workaround for local dev — a fix that never made it into the dashboard's user-facing generated command. This directly breaks the governing "magical install, no dialing into a machine" bar for the single most common recovery scenario: a node that isn't the controller's own host. See [[install-repair-one-liner-broken-when-controller-not-self-reachable]]. |
| 36 | Node shell (web terminal / break-glass host access) | ❌ **Critical — the button does nothing; a routing bug means the terminal page can never render** | Clicked "Node shell" on the degraded node's detail page. URL correctly changed to `/nodes/:id/terminal`, and network requests confirmed the terminal route's JS modules loaded (`200`s), but the rendered page never changed — still the exact same node-detail page (Repair panel, utilization chart, Details, Controls), no terminal window/modal/overlay anywhere. Confirmed not a client-side staleness artifact via a full hard navigation to the same URL — identical result. No console errors, no failed requests — a pure silent no-op. Root-caused via source: TanStack Router's flat-file convention makes `nodes/$nodeId.terminal.tsx` a *child route* of `nodes/$nodeId.tsx` (the dot implies nesting), and a parent only renders a matched child through `<Outlet />` — which `$nodeId.tsx` never includes. The child route still matches and its module still loads, it just has nowhere to render into. Confirmed via two other patterns already correctly used elsewhere in this same codebase: `services/$serviceId_.terminal.tsx` (trailing underscore) deliberately opts out of nesting, and `stacks/$name.tsx` correctly has `<Outlet />` for its own nested tab routes — the nodes pairing uses neither fix. Blocks all downstream testing of the terminal feature itself (approval flow, recording, `SWARMY_ALLOW_NODE_SHELL` gating, the live WebSocket session). See [[node-shell-terminal-never-renders-missing-outlet]]. |
| 37 | REST API surface (Settings → API keys) + licensing | ✅ **Works** | Minted a real API key via the real "New API key" form (`sweep-test-key`, read-only) — key shown once, key list updated live. Fetched the real "API reference" link (`/api/v1/docs`) — a genuine Scalar-rendered OpenAPI doc, backed by a real spec at `/api/v1/openapi.json` (31 documented paths: nodes, stacks, services, volumes, snapshots, backup-targets, dns, ingress, mesh routes, api-keys, audit export). Exercised it for real against the running controller: `GET /api/v1/nodes` and `GET /api/v1/stacks` with the minted key returned genuine live data (`200`); the same calls with no `Authorization` header correctly returned a proper RFC7807 problem-details `401`. Confirmed scope enforcement is real, not cosmetic: the read-only key got a clean `403 POLICY_DENIED` ("API key lacks \"write\" scope") on both `POST /stacks` and `POST /nodes/{id}/cordon`; minting a second key with "Allow writes" on correctly showed `read, write` in the scopes column. Revoked both test keys afterward via the real "Revoke" button — clean toast, status flipped to `revoked` immediately. One minor UX nit, consistent with a pattern already seen elsewhere in this sweep (row 12): clicking "Create key" with the Name field still empty (showing only placeholder text) silently no-ops — no toast, no validation message, no network call — not severity-worthy enough for its own issue file. Licensing: `apps/api/src/license.ts` is an explicit, self-documented stub ("THIS IS A STUB... lands with the first enterprise feature") — no EE features exist yet to gate, so there's nothing to test; correctly surfaces `enterprise: false` on the real `GET /version` endpoint. Nothing to fix here — this is the one feature area in the whole sweep that needed zero workarounds and zero source-reading to explain a bug. |

## Fix phase (2026-09-22) — every sweep blocker fixed in code

All open sweep findings were fixed in code in one pass; full-repo `typecheck` (26/26) and `test`
(22/22 suites, 0 fail) are green and the dashboard builds. **None of it is live-re-verified yet** —
that is the next phase (below). Each issue file carries a `## Fix applied (2026-09)` section.

| Sweep row | Issue | Fix |
|---|---|---|
| 8 | [[ingress-never-actually-serves-traffic]] | Placement uses the Docker swarm node id; config is delivered by exec into the running Caddy task (was: written to a host with no Caddy); 80/443 host-mode; enable/driver/targets changes converge the controller themselves; new `ingress-reconcile` loop; badges derive from live state (`serving`/`down`/`degraded`…), not saved config. Caddy admin API bound to loopback. |
| 9 | [[managed-postgres-bitnami-image-tag-16-dead]] | `bitnamilegacy/*` stopgap (replication verified live); single `DEFAULT_MANAGED_PG_IMAGE`; stuck clusters heal on re-provision. **Follow-up:** port to official `postgres` image. |
| 10/15/16/18 | [[dashboard-session-dies-under-normal-use]] | Root cause: default `better-auth.*` cookie names shared with every other Better Auth app on `localhost`. Now `swarmy.*` prefix; client re-checks once on 401 and returns you to where you were after re-login. |
| 11 | [[replicated-object-store-crashes-on-first-configure]] | `members()` null-safe. |
| 14 | [[backup-failure-reason-captured-but-never-shown]] | Failure reason rendered on every snapshot list (volume, controller, DB). |
| 17 | [[node-path-backup-destination-always-treated-as-s3]] | Enum case normalised **and** host path now bind-mounted into the restic sidecar (second latent bug: repo would have died with the container). |
| 19 | [[service-detail-containers-panel-shows-empty-despite-running]] | Hook matched by id only; "Ship a service" navigates by name. |
| 22 | [[controller-backup-restic-not-found-in-path]] | restic 0.16.4 bundled in the controller image; actionable error otherwise. |
| 26 | [[ci-build-trigger-always-fails-no-enable-path]] | "Builder" node role (label), toggle in node UI, single-node orgs default on; dispatch only to builder nodes with an actionable error. |
| 29 | [[org-signing-key-cosign-permission-denied]] | `RunOncePayload.user`; keygen runs as `0:0`. |
| 30 | [[observability-store-hostname-mismatch-always-unreachable]] | DSN uses `swarmy-clickhouse`; legacy rows heal on read. |
| 33 | [[guardrails-bypassed-by-quick-deploy-service-create]] | Shared `enforceAdmission` gate on createService, updateService, addServiceToStack, GUI builder, CI autodeploy, PR previews; audit rows for scale/restart/remove. |
| 35 | [[install-repair-one-liner-broken-when-controller-not-self-reachable]] | One `--controller <base>` threads through loader → installer → binary → dial-back; server derives base from request headers when `CONTROLLER_PUBLIC_URL` is loopback and warns; Repair mints a fresh 24h token. |
| 36 | [[node-shell-terminal-never-renders-missing-outlet]] | Route un-nested (`$nodeId_.terminal.tsx`). |
| 2 | [[stale-swarm-config-blocks-new-nodes-after-manager-loss]], [[worker-auto-rejoin-forms-standalone-swarm]] | Pure `planSwarmMembership`: join a live manager, else try stored join once, else re-elect; never init while a live manager is connected. |
| — | [[rejoin-force-can-corrupt-manager]] | Mitigated: bounded timeouts, stale-peer detection, docker restart + one retry. |
| — | [[dashboard-stats-not-org-scoped]] | Counts scoped to the org's nodes/stacks. |
| 7 | scratch files | `apps/api/diag-org*.ts` removed. |

### Known follow-ups (not blockers for the re-sweep)

- ~~CI `autodeployBuilt` redeploys with a bare spec, wiping live labels/env/ports/secrets~~ — fixed: rebuilds from live inspect via `promoteSpecFrom`, audits `cicd.autodeploy.failed` on failure.
- New orgs default ingress driver `NONE` (now honestly shown as "Tracking only") — product call: default to Caddy?
- `edge-per-node` topology: docker-install agent mounts a named volume at `/var/lib/swarmy`, so the host-path Caddyfile never reaches the edge task.
- `NodeDetail.swarmOrchestration` (waiting/joining/failed + reason) is exposed but not rendered.
- Org switch can show the previous org for up to 60s (cookie cache not refreshed by `switchOrg`).
- `/services/new` has no admin override toggle for guardrail blocks; REST `POST /services` 412 not in OpenAPI.
- Managed Postgres off Bitnami entirely.

### Next phase: live re-verification, then DigitalOcean

1. Re-run this sweep, product-surface only, on a fresh Lima VM + fresh org: rows 1-3, 8 (ingress: browser-reachable secured route), 9, 10, 11, 17, 22, 26, 29, 30, 33, 35, 36.
2. Two-node Lima: kill the manager VM, add a new node — must self-heal (row 2 issues).
3. DigitalOcean: 3 droplets / 2 regions — routing mesh (row 23), real public-IP ingress + ACME, geo-DNS (row 31).

## Live local verification (2026-09-22/23) — fresh Lima VMs, product surface + API

Fresh `swarmy-launch-1` (manager, containerised agent) + `swarmy-launch-2` (worker, systemd agent),
plus `swarmy-oss-1` installed with the **public** one-liner and **public GHCR images** only.

| Flow | Result |
|---|---|
| `curl …/install-swarmy.sh \| sudo bash` on a clean VM (no Docker, no checkout) | ✅ after 5 installer fixes (egress gate, empty secret, NAT login URL, stack file fetch, shared overlay) |
| Second node via dashboard one-liner; repair via the same one-liner | ✅ first try; repair keeps the node id |
| Deploy compose → add domain → HTTPS in browser | ✅ (routed services auto-join the edge overlay; private hosts get `tls internal`) |
| Managed Postgres primary + streaming replica across nodes | ✅ now on named volumes, pinned, anti-affine |
| Legacy (unmounted) cluster → Migrate storage | ✅ online pg_basebackup; all rows in `app` + a second DB intact, writable, survives restart |
| Cache, node-path backup, restore, controller self-backup | ✅ (controller backups refuse node-path targets) |
| swarmy object storage (Garage) → native S3 target → volume backup from the worker | ✅ Garage on the overlay, nothing published |
| CI: link repo → build on Builder node → push → other node pulls + runs | ✅ after 6 build fixes (`localhost:5000` registry) |
| Guardrails block `:latest` into prod; admin override | ✅ |
| Org signing key; Observability store | ✅ (configs via Docker configs; self-healed) |

### Open decisions (security)

- In-swarm registry is plain HTTP with **no auth**, published on :5000 on every node → auto-generate credentials on enable.
- Better Auth rate limiting can't see client IPs (shared bucket) → trust the socket IP / proxy header.
- Garage `garage.toml` and the collector config (secrets inside) are readable via `docker config inspect` → move to Docker secrets.

## Resolved decision: step-zero fix was authorized (2026-07-11)

The prior version of this section laid out three options for the GHCR blocker (stay blocked / ask
for one-time authorization / bypass via `local-vms.sh`) and deferred to the user rather than
deciding unilaterally. **The user chose option 2 — one-time authorization for the specific,
already-scoped fix** (repoint one URL, described above). It was applied and verified working for
that specific failure mode. See [[install-defaults-to-unpullable-ghcr-image]] for the full record.

That authorization was scoped to exactly that one change — it does not extend to the new blocker
in row #2 above. The same pattern (root-cause and document first, then ask before code-fixing) is
being followed again for that issue.

## Note on the overlay-networking blocker section below

The "Current blocker: Docker Swarm overlay networking is broken on both nodes" section further
down in this file describes the **earlier, two-node environment that was torn down** per the
methodology correction above (manual `rejoin --force` recovery, stale manager address, VXLAN
gossip failure). It's kept here for the underlying agent-code lesson it points at
([[rejoin-force-can-corrupt-manager]]'s suggested fix for `forceManagerReform()`), but it is not
the current blocker — the current blocker is row #2 in the status table, a completely different
(and earlier-in-the-flow) bug on a fresh single-node environment that never had a chance to reach
`rejoin --force` at all.

## Current blocker: Docker Swarm overlay networking is broken on both nodes

**This stops all further stack-deploy testing** and needs a decision before continuing.

### What happened

Earlier in this sweep, `swarmy-agent rejoin --force` on node-1 (recommended by the agent's own
`doctor` command, to move swarm traffic off a stale LAN IP onto the mesh IP) hung and left the
node in a broken swarm state (`ControlAvailable: false`). With explicit authorization, this was
recovered via `systemctl restart docker` + a `rejoin --force` retry, which succeeded, and node-2
was manually rejoined. Both nodes then showed `Ready` in `docker node ls`, mesh connectivity was
confirmed, and Task #1 above was marked done.

**That recovery was incomplete.** Investigating why the WordPress blueprint deploy failed
(`(HTTP code 500) server error - no VNI provided` — see reproduction below) found that:

- `docker info` on **both** nodes still shows the identical *stale* manager address
  (`100.71.150.60:2377` — a mesh IP NetBird reassigned away from node-1 days ago) in
  `RemoteManagers`, including on node-2, which explicitly joined against the *correct* address.
  This points at the manager's own raft-carried node record surviving the `--force-new-cluster`
  reform with the old address baked in, and every node inheriting it via gossip regardless of
  what address they actually dialed.
- Docker's overlay-network control plane (VXLAN ID allocation — separate from the Raft/gRPC
  control API that `docker node ls` uses) depends on that address to form its own gossip mesh.
  With it wrong, that mesh never stabilizes: `journalctl -u docker` shows `"initialized VXLAN UDP
  port to 4789"` repeating every ~5-10 seconds indefinitely on both nodes, instead of once at
  startup — a continuous crash-loop, not a transient hiccup.
- **Every new overlay network fails to create**, confirmed with a fresh, previously-unused name
  (`zztest-net`) and after removing all leftover orphaned networks from earlier org tests
  (`blog-net`, `gpgog_db-net`) — ruling out a name/VNI-specific conflict.

Full writeup: [[rejoin-force-can-corrupt-manager]] (updated 2026-07-10 with this finding).

### Reproduction

1. Dashboard → Deploy → Blueprints → WordPress → name `wptest`, size S, no domain → Deploy.
2. Secret `wptest-db-password` is created successfully.
3. Stack deploy step fails: `(HTTP code 500) server error - no VNI provided`.
4. Confirmed independently via `docker network create --driver overlay --attachable zztest-net`
   on node-1 directly — same error, no swarmy involved.

### Why this needs a decision rather than another silent fix

The realistic fix is a **full swarm teardown and clean rebuild** — `docker swarm leave --force`
on both nodes, then a fresh `docker swarm init --advertise-addr <node-1 mesh IP>` (not
`--force-new-cluster`, which is what got us into this state) followed by a normal `docker swarm
join` from node-2. That's the same class of irreversible local-destruction action already
authorized once this session. It's low-risk right now (0 real services/data on either node — only
test secrets and an aborted `wptest` deploy), but it needs the same explicit go-ahead, since it's
a repeat of a destructive recovery action, not a routine fix.

**Not proposing this as a swarmy code fix** — this is Docker Engine's own overlay-network gossip
state, not something swarmy's installer/agent code caused or can repair from the outside. The
underlying swarmy code bug this traces back to is `apps/agent/src/cli/rejoin.ts`'s
`forceManagerReform()` using `--force-new-cluster`, which Docker itself documents as a
last-resort that doesn't guarantee full object integrity afterward (see suggested fix in
[[rejoin-force-can-corrupt-manager]] — add a bounded timeout/stale-peer detection there so this
situation is avoided rather than needing recovery).

### Options going forward

1. **Rebuild the swarm from scratch** (recommended) — clean `leave --force` + `init` on both
   nodes, re-enroll is not needed (swarmy-level node identity is separate from Docker swarm
   membership, confirmed by node-2's `reconnect` correctly reusing its existing swarmy node ID
   through the earlier recovery). Unblocks all remaining stack-deploy testing.
2. **Provision fresh VMs** instead of repairing these — slower, but guarantees no leftover state
   from this session's multiple recovery attempts and earlier org-switch testing.
3. **Pause the stack-deploy portion of this sweep** and finish auditing the non-deploy-dependent
   surfaces first (auth/ABAC, REST API surface docs, licensing, UI-only flows), coming back to
   deploy testing once a decision is made.

Waiting on the user's call before taking any of these — all three are reasonable, but #1 and #2
both involve destructive actions on the test VMs.
