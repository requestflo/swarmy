# issues/

Findings from the launch readiness sweep: one file per bug, each with a root cause, a fix and a **Status** line. Open findings stay at the top level of this folder. Files whose Status is fixed or mitigated move to [`resolved/`](./resolved/), which keeps the history, including fixes still waiting on live re-verification.
