// Ingress. The org's edge config (driver + settings) lives in the org's swarm
// (swarm-kv `ingress/<orgId>`, plans/epic-docker-native-state.md P4); this enum
// remains for Stack.ingressDriver.

enum IngressDriver {
  CADDY
  TRAEFIK
  NONE
  CLOUDFLARE_TUNNEL
  NGINX
  HAPROXY
}
