{
  "name": "@swarmy/dns",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "exports": {
    ".": { "types": "./src/index.ts", "import": "./src/index.ts" }
  },
  "scripts": {
    "build": "echo '@swarmy/dns resolves to src'",
    "test": "bun test",
    "typecheck": "tsc --noEmit",
    "clean": "rm -rf dist .turbo"
  },
  "dependencies": {
    "@swarmy/core": "workspace:*",
    "dns-packet": "^5.6.1",
    "zod": "^3.24.1"
  },
  "devDependencies": {
    "@types/dns-packet": "^5.6.5",
    "typescript": "^5.5.4"
  }
}
